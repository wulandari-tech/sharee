<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
  <style>
    body { font-family: 'Inter', sans-serif; background-color: #111827; }
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');
    .form-input {
      @apply w-full p-3.5 rounded-lg bg-gray-700 border border-gray-600 text-gray-100 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition-shadow placeholder-gray-400;
    }
    .form-label {
      @apply block text-sm font-medium text-indigo-300 mb-1.5;
    }
    .auth-card {
      background: rgba(17, 24, 39, 0.8); /* bg-gray-900 with opacity for a "glassmorphism" hint */
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
      border: 1px solid rgba(55, 65, 81, 0.5); /* border-gray-700 with opacity */
    }
    .gradient-border-bottom::after {
        content: '';
        display: block;
        width: 50px;
        height: 3px;
        background: linear-gradient(90deg, #818cf8, #a78bfa); /* indigo-300 to purple-300 */
        margin-top: 0.5rem;
        border-radius: 2px;
    }
  </style>
</head>
<body class="text-gray-200 min-h-screen flex flex-col items-center justify-center p-4 bg-gradient-to-br from-gray-900 via-purple-900 to-indigo-900">
  <%- include('partials/_messages') %>
  
  <a href="/" class="absolute top-6 left-6 text-indigo-300 hover:text-indigo-100 transition-colors">
    <i class="fas fa-home mr-1"></i> Beranda
  </a>

  <div class="w-full max-w-md">
    <div class="auth-card shadow-2xl rounded-xl p-8 md:p-10">
      <div class="text-center mb-8">
        <i class="fas fa-rocket fa-3x text-indigo-400 mb-4"></i>
        <h1 class="text-3xl font-bold text-white gradient-border-bottom inline-block">Login</h1>
        <p class="text-gray-400 mt-2">Selamat datang kembali! Masuk untuk melanjutkan.</p>
      </div>
      
      <form action="/auth/login" method="POST" class="space-y-6">
        <div>
          <label for="email" class="form-label">Email</label>
          <div class="relative">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <i class="fas fa-envelope text-gray-400"></i>
            </div>
            <input type="email" name="email" id="email" placeholder="email@example.com" required class="form-input pl-10" />
          </div>
        </div>
        
        <div>
          <label for="password" class="form-label">Password</label>
          <div class="relative">
             <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
              <i class="fas fa-lock text-gray-400"></i>
            </div>
            <input type="password" name="password" id="password" placeholder="••••••••" required class="form-input pl-10" />
          </div>
        </div>

        <button type="submit" class="w-full bg-gradient-to-r from-indigo-600 to-purple-600 hover:from-indigo-700 hover:to-purple-700 text-white font-bold px-6 py-3.5 rounded-lg transition-all duration-300 ease-in-out transform hover:scale-105 shadow-lg focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-gray-800 focus:ring-indigo-500">
          <i class="fas fa-sign-in-alt mr-2"></i> Login
        </button>
      </form>
      
      <p class="mt-8 text-center text-gray-400 text-sm">
        Belum punya akun? 
        <a href="/auth/register" class="font-medium text-indigo-400 hover:text-indigo-300 transition-colors">Daftar di sini</a>
      </p>
    </div>
  </div>
  <footer class="mt-8 text-center text-sm text-indigo-300 opacity-75">
    © <%= new Date().getFullYear() %> SHARECODE.
  </footer>
</body>
</html>